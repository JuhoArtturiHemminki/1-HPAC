[package]
name = "one_hpac"
version = "1.0.42"
edition = "2021"

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
codegen-units = 1
strip = true

[dependencies]
uefi = { version = "0.24.0", default-features = false }
